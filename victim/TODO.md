# TODO (Alpha milestone)
# This directory will contain:
#   nginx.conf      — web server config for the victim service
#   entrypoint.sh   — starts nginx + tcpdump simultaneously, saves .pcap to /pcaps

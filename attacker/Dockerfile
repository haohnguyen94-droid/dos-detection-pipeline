FROM python:3.11-slim

# TODO (Alpha): install hping3, scapy for attack scripts
# RUN apt-get update && apt-get install -y hping3 && pip install scapy

WORKDIR /attacker
COPY . .

# Placeholder entrypoint — replaced at Alpha milestone
CMD ["echo", "[attacker] stub — attack scripts not yet implemented"]

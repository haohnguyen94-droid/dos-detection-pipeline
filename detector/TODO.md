# TODO (Beta milestone)
# This directory will contain:
#   detector.py     — main entry point, orchestrates analysis
#   features.py     — extracts per-flow stats from PCAP (SYN rate, UDP rate, flow duration)
#   rules.py        — rule-based detection signatures for each attack type
#   report.py       — generates HTML/JSON detection report
#   requirements.txt

FROM python:3.11-slim

# TODO (Beta): install scapy, jinja2 for rule engine and reporting
# RUN pip install scapy jinja2

WORKDIR /detector
COPY . .

# Placeholder — replaced at Beta milestone
CMD ["echo", "[detector] stub — rule engine not yet implemented"]
